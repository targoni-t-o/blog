<%= simple_form_for([@article, @article.comments.build]) do |f| %>
    <div>
      <%= f.text_area :body, placeholder: 'Comment text' %>
    </div>

    <div>
      <%= f.button :submit %>
    </div>
<% end %>