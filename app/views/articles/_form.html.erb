<%= simple_form_for @article do |f| %>
    <%= f.input :title %>
    <%= f.input :text, as: :ckeditor, ckeditor: { language: 'en'} %>
    <%= f.button :submit, class: 'btn btn-primary' %>
    <%= link_to 'Back', articles_path, class: 'btn btn-default' %>
<% end %>